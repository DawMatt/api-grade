# Test fixture — references an undefined custom function; any run against this ruleset is expected to fail
rules:
  missing-function-test:
    id: missing-function-test
    description: Test missing function handling
    given: $
    formats:
      - oas3
      - oas3_1
    type: validation
    severity: error
    then:
      function: missing-function