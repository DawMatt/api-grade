# Implementation Plan: Publish Packages to npmjs

**Branch**: `006-publish-npmjs` | **Date**: 2026-06-16 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `specs/006-publish-npmjs/spec.md`

## Summary

Publish all four `@dawmatt`-scoped packages to the npmjs public registry and automate the entire release pipeline via GitHub Actions. The pipeline enforces quality gates (tests, linting, type checking, build, coverage threshold, dependency audit) on every code change and before every release. Only maintainers may trigger a release via protected tags. The full release process — including version assignment and recovery steps — is documented in the contribution guide.

## Technical Context

**Language/Version**: TypeScript 5.4, Node.js ≥ 20.0.0 (ESM)

**Package Manager**: Yarn 1.22 (classic workspaces); four publishable packages at root + `packages/*`

**Packages to Publish** (in dependency order):
1. `api-grade-core` → `@dawmatt/api-grade-core` (`packages/api-grade-core`)
2. `backstage-plugin-api-grade` → `@dawmatt/backstage-plugin-api-grade` (`packages/backstage-plugin-api-grade`)
3. `backstage-plugin-api-grade-backend` → `@dawmatt/backstage-plugin-api-grade-backend` (`packages/backstage-plugin-api-grade-backend`)
4. `api-grade` (root) → `@dawmatt/api-grade` (CLI)

**Internal Dependency Chain**: Packages 2, 3, and 4 depend on `api-grade-core: "*"` (workspace reference). These must be rewritten to `@dawmatt/api-grade-core: "^<version>"` before publishing.

**Testing**: Vitest + @vitest/coverage-v8; separate configs per package and at root. No coverage thresholds set — will be established as part of this feature.

**Linting**: No linting tooling currently exists. ESLint with TypeScript support will be introduced as part of quality gates.

**CI/CD Platform**: GitHub Actions (no existing workflows; all workflows are net-new)

**Target Platform**: npmjs public registry

**Constraints**: All prerequisites must be free (constitution V); no proprietary tooling

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Multi-Format API Support | ✅ Pass | Feature does not touch grading logic; no format support regression risk |
| II. Core-First Architecture | ✅ Pass | All four packages published independently; no logic duplication introduced |
| III. Spectral-Ruleset Based Grading | ✅ Pass | Pipeline validates existing behaviour; does not modify grading algorithm |
| IV. Test-Driven Quality | ✅ Pass | Quality gate enforces tests pass before any publish; coverage threshold added |
| V. Cross-Platform & Zero-Cost Prerequisites | ✅ Pass | npmjs public packages are free; GitHub Actions free tier covers CI/CD |
| VI. Educational Excellence | ✅ Pass | No change to sample APIs or diagnostic output |
| CI/CD Integration | ✅ Pass | Automated pipeline; maintainer-only; quality-gated; no manual credential handling |
| YAGNI | ✅ Pass | Pipeline covers only what is needed now; no future-proofing abstractions |
| Development Workflow | ✅ Pass | Feature branch + PR; test suite runs in CI |

**Post-Phase-1 re-check**: Required after package name and pipeline contracts are finalised (confirm no Core-First violation from publishing structure).

## Project Structure

### Documentation (this feature)

```text
specs/006-publish-npmjs/
├── plan.md              # This file
├── research.md          # Phase 0 decisions
├── data-model.md        # Package catalog and pipeline stage definitions
├── contracts/
│   └── package-names.md # Definitive @dawmatt package name registry
└── tasks.md             # Generated by /speckit-tasks
```

### Source Code (additions this feature)

```text
.github/
└── workflows/
    ├── ci.yml           # Quality gate on every PR / push
    └── release.yml      # Maintainer-triggered publish pipeline

packages/
├── api-grade-core/
│   └── package.json     # name → @dawmatt/api-grade-core
├── backstage-plugin-api-grade/
│   └── package.json     # name → @dawmatt/backstage-plugin-api-grade
└── backstage-plugin-api-grade-backend/
    └── package.json     # name → @dawmatt/backstage-plugin-api-grade-backend

package.json             # name → @dawmatt/api-grade (CLI)
.npmrc                   # Scoped registry config; provenance flag
eslint.config.mjs        # ESLint flat config with TypeScript support
.github/
└── release-process.md   # Canonical release guide (linked from CONTRIBUTING.md)

docs/
├── getting-started.md   # Updated: npm install instructions per package
├── package/             # Updated: @dawmatt/api-grade-core install + usage
├── cli/                 # Updated: @dawmatt/api-grade install + usage
└── backstage-plugins/   # Updated: both plugin packages, install steps
```

**Structure Decision**: Monorepo with Yarn workspaces retained as-is. New files are additions only (CI workflows, ESLint config, .npmrc). Package names are updated in package.json files. No directory restructuring required.

## Quality Gate Requirement (Constitution Constraint)

Per the constitution's Development Workflow section, `/speckit-implement` MUST NOT
report any task or phase complete until all six CI quality gate stages pass locally:

```sh
npm audit --audit-level=high --omit=dev
npm run lint
npm run typecheck --workspaces --if-present
npm run test:coverage                                      # root
yarn workspace api-grade-core run test:coverage
yarn workspace backstage-plugin-api-grade run test:coverage
yarn workspace backstage-plugin-api-grade-backend run test:coverage
npm run build
```

If any stage exits non-zero, the task is **not done**. Fix the failure, re-run the
gate, and only then mark the task complete and commit.

> **Interim gate (Phase 1 only)**: Phase 2 installs lint and coverage scripts. For
> Phase 1 tasks, run `npm run build` and `npm run typecheck` as a minimum gate until
> Phase 2 tooling is in place.

## Complexity Tracking

No constitution violations. No unjustified abstractions introduced.
